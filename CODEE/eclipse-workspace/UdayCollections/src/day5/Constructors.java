/*
 * Constructors :
 * ---------------
 * 
 * HashSet()// default capacity is 16
 * HashSet(int initialCapacity)// if we give initialCapacity 20 then its default incremental capacity will be load factor of 0.75 or 75%
 * 									 of its current capacity.
 * HashSet(int initialCapacity, float loadFactor)//here we can only give the load factor what we want for eg : HashSet(10, 1.5)
 * HashSet(collection c)
 * 
 * 
 * LinkedHashSet()// default capacity is 16
 * LinkedHashSet(int initialCapacity)// if we give initialCapacity 20 then its default incremental capacity will be load factor of 0.75 or 75%
 * 									 of its current capacity.
 * LinkedHashSet(int initialCapacity, float loadFactor)//here we can only give the load factor what we want for eg : HashSet(10, 1.5)
 * LinkedHashSet(collection c)
 * 
 * 
 * 
 * 
 */